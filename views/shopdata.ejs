<%-include("header.ejs")%>
    <main class="main">
<h1><%=shopname%> Products</h1>
<p>List of all products</p>
<% if(products.length>0){ %>
<% products.forEach((product)=>{ %>
    <li><%=product.name%></li>
    <% }) %>
    <% } else { %>
        <p>no data</p>
        <% } %>
</main>
</body>
</html>